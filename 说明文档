不用管这个文件
main
    启动UI（整个系统）
        UI设计（也可以作为一个包导入？？）
            将前后端分离？有道理的
    为什么这么启动呢？？？
        你想想，要使用我们这个系统，直接利用该接口启动就完了
        或者说main中可以包含一个多系统
    （扩展接口）

visualization
    UI.py（只提供展示界面的功能！！）
        扩展性：直接加函数
        提供接口（提供个pi，不需要提供任何接口！！！！！）



            bububub,这些功能，应该由功能接口提供
            抓取数据包的功能接口
            其他按钮的功能接口
        可维护：直接修改接口，和函数
        结构：
            初始化：无需接口，这是内部自己写啊
